XAI_API_KEY=your_api_key_here

# Optional local checkpoint directory (default: ~/.cache/grok-search-mcp).
# GROK_SEARCH_DATA_DIR=/absolute/path/to/private/research-data
# Load this file explicitly: uv run --env-file .env grok-search-mcp
